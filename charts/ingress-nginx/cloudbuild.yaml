options:
  # Ignore Prow provided substitutions.
  substitution_option: ALLOW_LOOSE
steps:
- name: gcr.io/cloud-builders/docker
  dir: charts
  env:
  - NAME=ingress-nginx
  entrypoint: make
  args:
  - push

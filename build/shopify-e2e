#!/bin/bash
set -euo pipefail

if [[ -n "${DEBUG:+set}" ]]; then
  set -x
fi

_term() {
  echo "Test aborted via SIGTERM - was the CI instance preempted?"
  exit 1
}
trap _term SIGTERM

make e2e-test-image
make e2e-test
